spring:
  config:
    import: application-aws.yml

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  servlet:
    session:
      cookie:
        same-site: none
        secure: true
